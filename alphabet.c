#include<stdio.h>
int main()
{
  int input;
  printf("Enter the input\n");
  scanf("%c",&c);
  if((c >='A' && c >='z') || (c >= 'a' && c <= 'z'))
  {
    printf("Alphabet");
  }
  else
  {
    printf("No");
   }
  return 0;
}
   
